bash
-lc
cd ~/temporal-straightening; OFF_NAME=wall_False_agg32_projchannel_dim8_hw14_sgTrue_lr1e-06; ON_NAME=wall_aggmlpcos1e-1_agg32_projchannel_dim8_hw14_sgTrue_lr1e-05; OFF_BASE=$PWD/baseline_artifacts/checkpoints/wall_projector_off_paper_v3/test; ON_BASE=$PWD/baseline_artifacts/checkpoints/wall_projector_on_paper_v4/test; OFF_FILE=$OFF_BASE/$OFF_NAME/checkpoints/model_20.pth; ON_FILE=$ON_BASE/$ON_NAME/checkpoints/model_20.pth; OUT_ROOT=$PWD/baseline_artifacts/plans/wall_dino_projector_full; mkdir -p $OUT_ROOT baseline_artifacts/logs; echo WATCHER_STARTED $(date -Is); while [ ! -s $OFF_FILE ] || [ ! -s $ON_FILE ]; do echo WAITING $(date -Is) OFF=$(test -s $OFF_FILE && echo yes || echo no) ON=$(test -s $ON_FILE && echo yes || echo no); sleep 60; done; while kill -0 219988 2>/dev/null || kill -0 227264 2>/dev/null; do echo WAITING_TRAINING $(date -Is); sleep 60; done; echo CHECKPOINTS_READY $(date -Is); sleep 120; run_worker(){ slot=$1; idx=0; for cond in off on; do if [ $cond = off ]; then base=$OFF_BASE; name=$OFF_NAME; else base=$ON_BASE; name=$ON_NAME; fi; for seed in 100 200 300; do for offset in 0 10 20 30 40; do if [ $((idx % 7)) -eq $slot ]; then out=$OUT_ROOT/$cond/seed_$seed/chunk_$offset; mkdir -p $out; if [ -s $out/logs.json ] && grep -q final_eval/success_rate $out/logs.json; then echo SKIP $cond $seed $offset; else echo START $(date -Is) GPU=$((slot+1)) $cond $seed $offset; env CUDA_VISIBLE_DEVICES=$((slot+1)) CPATH=$HOME/.conda/envs/ts/include LIBRARY_PATH=$HOME/.conda/envs/ts/lib LD_LIBRARY_PATH=$HOME/.mujoco/mujoco210/bin:$HOME/.conda/envs/ts/lib:$LD_LIBRARY_PATH:/usr/lib/nvidia MUJOCO_PY_MUJOCO_PATH=$HOME/.mujoco/mujoco210 DATASET_DIR=$HOME/ts_data/data WANDB_MODE=disabled HYDRA_FULL_ERROR=1 conda run -n ts --no-capture-output python plan.py --config-name plan_gd.yaml ckpt_base_path=$base model_name=$name model_epoch=20 n_evals=10 +wandb_logging=false seed=$seed eval_start_index=$offset hydra.run.dir=$out > $out/runner.log 2>&1; rc=$?; echo END $(date -Is) RC=$rc GPU=$((slot+1)) $cond $seed $offset; fi; fi; idx=$((idx+1)); done; done; done; }; for slot in 0 1 2 3 4 5 6; do run_worker $slot & done; wait; echo PLANNING_FINISHED $(date -Is)
